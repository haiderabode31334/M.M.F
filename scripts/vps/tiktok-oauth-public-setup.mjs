import{spawn}from'node:child_process';import os from'node:os';import path from'node:path';
const C='/snap/chromium/current/usr/lib/chromium-browser/chrome',P=path.join(os.homedir(),'mmf-browser-profile'),PORT=9264,A='M.M.F Publisher',N='M.M.F Test',REDIRECT='https://haiderabode31334.github.io/M.M.F/callback.html',sleep=m=>new Promise(r=>setTimeout(r,m));
const ch=spawn(C,['--headless=new','--no-sandbox','--disable-gpu','--disable-dev-shm-usage',`--user-data-dir=${P}`,`--remote-debugging-port=${PORT}`,'--remote-debugging-address=127.0.0.1','--remote-allow-origins=*','https://developers.tiktok.com/apps'],{stdio:['ignore','ignore','ignore']});let ws,id=0;const q=new Map();function send(method,params={}){const n=++id;ws.send(JSON.stringify({id:n,method,params}));return new Promise((r,j)=>q.set(n,{r,j}))}async function ev(x){const z=await send('Runtime.evaluate',{expression:x,returnByValue:true,awaitPromise:true});if(z?.exceptionDetails)throw Error('EVAL_EXCEPTION');return z?.result?.value}async function txt(){return String(await ev('document.body?.innerText||""')||'')}async function click(t){return ev(`(()=>{const v=e=>!!(e&&(e.offsetWidth||e.offsetHeight||e.getClientRects().length)),q=${JSON.stringify(t)},es=[...document.querySelectorAll('button,a,[role="button"],div,span')].filter(v).filter(x=>(x.innerText||x.textContent||'').replace(/\\s+/g,' ').trim()===q&&!x.disabled);if(!es.length)return 0;const e=es.at(-1);(e.closest('button,a,[role="button"]')||e).click();return 1})()`)}async function openApp(){for(let i=0;i<70;i++){if(String(await ev('location.pathname')||'').startsWith('/app/'))return 1;if(await click(A))await sleep(700);else await sleep(250)}return 0}async function sandbox(){for(let i=0;i<60;i++){if((await txt()).includes('You are editing '+N))return 1;if(await click('Sandbox')){await sleep(900);break}await sleep(250)}for(let i=0;i<60;i++){if((await txt()).includes('You are editing '+N))return 1;if(await click(N))await sleep(800);else await sleep(250)}return 0}async function setVisibleInput(index,val){return ev(`(()=>{const es=[...document.querySelectorAll('input,textarea')].filter(e=>e.offsetWidth||e.offsetHeight||e.getClientRects().length),e=es[${index}],v=${JSON.stringify(val)};if(!e)return 0;const proto=e.tagName==='TEXTAREA'?HTMLTextAreaElement.prototype:HTMLInputElement.prototype,s=Object.getOwnPropertyDescriptor(proto,'value')?.set;e.focus();s?s.call(e,v):e.value=v;e.dispatchEvent(new InputEvent('input',{bubbles:true,inputType:'insertText',data:v}));e.dispatchEvent(new Event('change',{bubbles:true}));return 1})()`)}
try{let page;for(let i=0;i<100;i++){try{const a=await(await fetch(`http://127.0.0.1:${PORT}/json`)).json();page=a.find(x=>x.type==='page');if(page?.webSocketDebuggerUrl)break}catch{}await sleep(200)}if(!page)throw Error('CDP_NOT_READY');ws=new WebSocket(page.webSocketDebuggerUrl);await new Promise((r,j)=>{ws.onopen=r;ws.onerror=j});ws.onmessage=e=>{const m=JSON.parse(e.data);if(m.id&&q.has(m.id)){const p=q.get(m.id);q.delete(m.id);m.error?p.j(Error(m.error.message)):p.r(m.result)}};await send('Runtime.enable');await sleep(5000);if(!await openApp())throw Error('APP_NOT_FOUND');if(!await sandbox())throw Error('SANDBOX_NOT_ACTIVE');await sleep(1700);await ev('window.scrollTo(0,0)');await sleep(500);const fields=JSON.parse(await ev(`JSON.stringify([...document.querySelectorAll('input,textarea')].filter(e=>e.offsetWidth||e.offsetHeight||e.getClientRects().length).map((e,i)=>({i,type:e.type||'',len:String(e.value||'').length,match:String(e.value||'')===${JSON.stringify(REDIRECT)}})))`)||'[]');let match=fields.some(x=>x.match);console.log('REDIRECT_MATCH_BEFORE='+Number(match));if(!match){let ri=fields.find(x=>x.i===9&&x.type==='text')?.i;if(ri==null){const cs=fields.filter(x=>x.type==='text'&&x.len>=40&&x.len<=120);ri=cs.at(-1)?.i}if(ri==null||!await setVisibleInput(ri,REDIRECT))throw Error('REDIRECT_FIELD_NOT_FOUND');await sleep(400);let saved=0;for(const t of['Apply changes','Save changes','Save'])if(await click(t)){saved=1;break}console.log('REDIRECT_SAVE_CLICK='+saved);if(!saved)throw Error('REDIRECT_SAVE_NOT_FOUND');await sleep(3000);match=!!JSON.parse(await ev(`JSON.stringify([...document.querySelectorAll('input,textarea')].filter(e=>e.offsetWidth||e.offsetHeight||e.getClientRects().length).some(e=>String(e.value||'')===${JSON.stringify(REDIRECT)}))`)||'false')}console.log('CALLBACK_SAVED='+Number(match));if(!match)throw Error('REDIRECT_NOT_PERSISTED');const shape=JSON.parse(await ev(`JSON.stringify((()=>{const norm=e=>(e?.innerText||e?.textContent||'').replace(/\\s+/g,' ').trim(),mask=s=>{s=String(s||'').trim();if(/^[A-Za-z0-9._-]{8,}$/.test(s))return '[token:'+s.length+']';return s.slice(0,80)},lab=[...document.querySelectorAll('div,span,p,label,strong')].find(e=>norm(e)==='Client key');if(!lab)return{found:false};let root=lab;for(let i=0;i<5&&root.parentElement;i++){const t=norm(root.parentElement);if(/client secret/i.test(t))break;root=root.parentElement}const nodes=[root,...root.querySelectorAll('*')].slice(0,50).map(e=>({tag:e.tagName,text:mask(norm(e)),aria:mask(e.getAttribute?.('aria-label')),title:mask(e.getAttribute?.('title')),role:e.getAttribute?.('role')||'',type:e.getAttribute?.('type')||'',valueLen:'value'in e?String(e.value||'').length:0}));return{found:true,nodes}})())`)||'{}');console.log('CLIENT_KEY_NODE_SHAPE='+JSON.stringify(shape));}catch(e){console.log('STATUS=ERROR');console.log('ERROR='+String(e.message||e));process.exitCode=1}finally{try{ws?.close()}catch{}try{ch.kill('SIGKILL')}catch{}}
