import { spawn } from 'node:child_process';
import os from 'node:os';
import path from 'node:path';
const CHROME='/snap/chromium/current/usr/lib/chromium-browser/chrome';
const PROFILE=path.join(os.homedir(),'mmf-browser-profile');
const PORT=9233;const APP_NAME='M.M.F Publisher';const ICON='/tmp/mmf-tiktok-icon.png';const sleep=ms=>new Promise(r=>setTimeout(r,ms));
const DESCRIPTION='M.M.F Publisher is a web application that lets authenticated users connect their TikTok account and publish user-selected video content to TikTok through the official TikTok Content Posting API.';
const TERMS='https://haiderabode31334.github.io/M.M.F/terms.html';
const PRIVACY='https://haiderabode31334.github.io/M.M.F/privacy.html';
const WEBSITE='https://haiderabode31334.github.io/M.M.F/';
const REVIEW='Login Kit: users authorize their TikTok account through TikTok OAuth. Content Posting API: after authorization, users explicitly select content and publish it to their TikTok account. The user.info.basic scope is used only to show the connected account identity, and video.publish is used only after an explicit publish action by the user.';
const child=spawn(CHROME,['--headless=new','--no-sandbox','--disable-gpu','--disable-dev-shm-usage','--no-first-run','--no-default-browser-check','--window-size=1440,1100',`--user-data-dir=${PROFILE}`,`--remote-debugging-port=${PORT}`,'--remote-debugging-address=127.0.0.1','--remote-allow-origins=*','about:blank'],{stdio:['ignore','ignore','ignore']});
let ws,seq=0;const pending=new Map();function send(method,params={}){const id=++seq;ws.send(JSON.stringify({id,method,params}));return new Promise((res,rej)=>pending.set(id,{resolve:res,reject:rej}));}
async function ev(expression){const r=await send('Runtime.evaluate',{expression,returnByValue:true,awaitPromise:true});return r?.result?.value;}
async function ready(){for(let i=0;i<120;i++){try{const r=await fetch(`http://127.0.0.1:${PORT}/json`);if(r.ok)return await r.json();}catch{}await sleep(250);}throw new Error('CDP_NOT_READY');}
async function nav(u,ms=5000){await send('Page.navigate',{url:u});await sleep(ms);}async function clickExact(s){return await ev(`(()=>{const v=e=>!!(e&&(e.offsetWidth||e.offsetHeight||e.getClientRects().length));const q=${JSON.stringify(s)};let e=[...document.querySelectorAll('a,button,[role="button"],label,div,span')].filter(v).find(x=>(x.innerText||x.textContent||'').trim()===q);if(!e)return 0;(e.closest('a,button,[role="button"],label')||e).click();return 1})()`);}
async function setControlByLabel(label,value){return await ev(`(()=>{const q=${JSON.stringify(label)},val=${JSON.stringify(value)},v=e=>!!(e&&(e.offsetWidth||e.offsetHeight||e.getClientRects().length));const nodes=[...document.querySelectorAll('label,div,span,p')].filter(v);const n=nodes.find(x=>(x.innerText||x.textContent||'').replace(/\\s+/g,' ').trim().startsWith(q));if(!n)return 0;let c=n;let e=null;for(let i=0;i<6&&c;i++,c=c.parentElement){e=c.querySelector?.('input:not([type="checkbox"]):not([type="radio"]):not([type="file"]),textarea');if(e)break;}if(!e)return 0;const proto=e.tagName==='TEXTAREA'?HTMLTextAreaElement.prototype:HTMLInputElement.prototype;const set=Object.getOwnPropertyDescriptor(proto,'value')?.set;e.focus();set?set.call(e,val):e.value=val;e.dispatchEvent(new Event('input',{bubbles:true}));e.dispatchEvent(new Event('change',{bubbles:true}));return 1})()`);}
async function setUnlabelledTextarea(value){return await ev(`(()=>{const val=${JSON.stringify(value)},v=e=>!!(e&&(e.offsetWidth||e.offsetHeight||e.getClientRects().length));const ts=[...document.querySelectorAll('textarea')].filter(v).filter(e=>!e.placeholder);const e=ts.find(x=>{let p=x.parentElement;for(let i=0;i<5&&p;i++,p=p.parentElement){if(/Explain how each product and scope works/i.test(p.innerText||''))return true;}return false;})||ts[0];if(!e)return 0;const set=Object.getOwnPropertyDescriptor(HTMLTextAreaElement.prototype,'value')?.set;e.focus();set?set.call(e,val):e.value=val;e.dispatchEvent(new Event('input',{bubbles:true}));e.dispatchEvent(new Event('change',{bubbles:true}));return 1})()`);}
async function setCheckbox(label,checked=true){return await ev(`(()=>{const q=${JSON.stringify(label)},want=${checked?'true':'false'},v=e=>!!(e&&(e.offsetWidth||e.offsetHeight||e.getClientRects().length));const labels=[...document.querySelectorAll('label')].filter(v);let l=labels.find(x=>(x.innerText||x.textContent||'').trim()===q);let e=l?.querySelector('input[type="checkbox"]');if(!e){e=[...document.querySelectorAll('input[type="checkbox"]')].filter(v).find(x=>{let p=x.parentElement;for(let i=0;i<4&&p;i++,p=p.parentElement){if((p.innerText||'').trim()===q)return true;}return false;});}if(!e)return 0;if(e.checked!==want)(l||e).click();return 1})()`);}
async function selectCategory(){const opened=await ev(`(()=>{const v=e=>!!(e&&(e.offsetWidth||e.offsetHeight||e.getClientRects().length));const nodes=[...document.querySelectorAll('label,div,span,p')].filter(v);const n=nodes.find(x=>(x.innerText||x.textContent||'').replace(/\\s+/g,' ').trim()==='Category *');if(!n)return 0;let c=n;for(let i=0;i<7&&c;i++,c=c.parentElement){const e=c.querySelector?.('[role="combobox"],button,input');if(e&&e!==n){e.click();return 1;}}return 0})()`);if(!opened)return 0;await sleep(700);for(let i=0;i<8;i++){const picked=await ev(`(()=>{const v=e=>!!(e&&(e.offsetWidth||e.offsetHeight||e.getClientRects().length));const q='Images & Videos';const els=[...document.querySelectorAll('[role="option"],[role="menuitem"],li,button,div,span')].filter(v);let e=els.find(x=>(x.innerText||x.textContent||'').replace(/\\s+/g,' ').trim()===q);if(!e)return 0;(e.closest('[role="option"],[role="menuitem"],li,button')||e).click();return 1})()`);if(picked)return 1;await sleep(400);}return 0;}
async function uploadIcon(){const r=await send('Runtime.evaluate',{expression:`(()=>{const v=e=>!!(e&&(e.offsetWidth||e.offsetHeight||e.getClientRects().length));const nodes=[...document.querySelectorAll('label,div,span,p')].filter(v);const n=nodes.find(x=>(x.innerText||x.textContent||'').replace(/\\s+/g,' ').trim()==='App icon *');if(n){let c=n;for(let i=0;i<7&&c;i++,c=c.parentElement){const f=c.querySelector?.('input[type="file"]');if(f)return f;}}return document.querySelector('input[type="file"]');})()`});const oid=r?.result?.objectId;if(!oid)return 0;await send('DOM.setFileInputFiles',{objectId:oid,files:[ICON]});await sleep(1200);return 1;}
async function waitWebsite(){for(let i=0;i<30;i++){const ok=await ev(`(()=>{const v=e=>!!(e&&(e.offsetWidth||e.offsetHeight||e.getClientRects().length));return [...document.querySelectorAll('input')].filter(v).some(e=>/website|url/i.test([e.placeholder,e.name,e.id,e.getAttribute('aria-label')].join(' '))||(()=>{let p=e.parentElement;for(let j=0;j<4&&p;j++,p=p.parentElement){if(/Website URL/i.test(p.innerText||''))return true;}return false})())})()`);if(ok)return 1;await sleep(400);}return 0;}
async function setWebsite(){return await ev(`(()=>{const val=${JSON.stringify(WEBSITE)},v=e=>!!(e&&(e.offsetWidth||e.offsetHeight||e.getClientRects().length));const es=[...document.querySelectorAll('input[type="text"],input[type="url"]')].filter(v);const e=es.find(x=>/website/i.test([x.placeholder,x.name,x.id,x.getAttribute('aria-label')].join(' '))||(()=>{let p=x.parentElement;for(let i=0;i<5&&p;i++,p=p.parentElement){if(/Website URL/i.test(p.innerText||''))return true;}return false})());if(!e)return 0;const set=Object.getOwnPropertyDescriptor(HTMLInputElement.prototype,'value')?.set;e.focus();set?set.call(e,val):e.value=val;e.dispatchEvent(new Event('input',{bubbles:true}));e.dispatchEvent(new Event('change',{bubbles:true}));return 1})()`);}
async function clickSave(){const enabled=await ev(`(()=>{const v=e=>!!(e&&(e.offsetWidth||e.offsetHeight||e.getClientRects().length));const b=[...document.querySelectorAll('button,[role="button"]')].filter(v).find(x=>(x.innerText||x.textContent||'').trim()==='Save');return !!b&&!b.disabled&&b.getAttribute('aria-disabled')!=='true'})()`);if(!enabled)return 0;return clickExact('Save');}
async function errors(){const raw=await ev(`JSON.stringify((()=>{const t=document.body?.innerText||'';return t.split(/\\n+/).map(x=>x.replace(/\\s+/g,' ').trim()).filter(x=>x&&/required|invalid|not verified|must|error|failed|unsupported/i.test(x)).slice(0,30)})())`);return JSON.parse(raw||'[]');}
try{const ts=await ready();const p=ts.find(x=>x.type==='page');ws=new WebSocket(p.webSocketDebuggerUrl);await new Promise((res,rej)=>{const t=setTimeout(()=>rej(new Error('WS_TIMEOUT')),10000);ws.onopen=()=>{clearTimeout(t);res()};ws.onerror=rej});ws.onmessage=e=>{const m=JSON.parse(e.data);if(m.id&&pending.has(m.id)){const p=pending.get(m.id);pending.delete(m.id);m.error?p.reject(new Error(m.error.message)):p.resolve(m.result)}};await send('Page.enable');await send('Runtime.enable');await send('DOM.enable');await nav('https://developers.tiktok.com/apps');const body=String(await ev(`document.body?.innerText||''`));if(/log in with your tiktok developer account/i.test(body)){console.log('AUTH=NOT_SAVED');process.exitCode=2}else{console.log('AUTH=OK');const o=await clickExact(APP_NAME);console.log('APP_OPENED='+o);if(!o)throw new Error('APP_NOT_FOUND');await sleep(5500);console.log('ICON_UPLOADED='+await uploadIcon());console.log('CATEGORY_SELECTED='+await selectCategory());console.log('DESCRIPTION_SET='+await setControlByLabel('Description *',DESCRIPTION));console.log('TERMS_SET='+await setControlByLabel('Terms of Service URL *',TERMS));console.log('PRIVACY_SET='+await setControlByLabel('Privacy Policy URL *',PRIVACY));console.log('WEB_SELECTED='+await setCheckbox('Web',true));console.log('WEBSITE_FIELD_READY='+await waitWebsite());console.log('WEBSITE_SET='+await setWebsite());console.log('REVIEW_TEXT_SET='+await setUnlabelledTextarea(REVIEW));await sleep(600);const saved=await clickSave();console.log('SAVE_CLICKED='+saved);if(saved)await sleep(5000);console.log('VALIDATION_HINTS='+JSON.stringify(await errors()));}}
catch(e){console.log('ERROR='+String(e.message||e));process.exitCode=1}finally{try{ws?.close()}catch{}try{child.kill('SIGTERM')}catch{}await sleep(300);try{child.kill('SIGKILL')}catch{}}
