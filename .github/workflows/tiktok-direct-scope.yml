name: M.M.F TikTok Direct Post Scope
on:
  push:
    branches: ["main"]
    paths:
      - "scripts/vps/tiktok-direct-scope.mjs"
      - ".github/workflows/tiktok-direct-scope.yml"
permissions:
  contents: read
concurrency:
  group: mmf-tiktok-direct-scope
  cancel-in-progress: true
jobs:
  configure:
    runs-on: [self-hosted, linux, x64]
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - name: Prepare browser
        shell: bash
        run: |
          sudo -n true
          pkill -u "$(id -u)" -f '[r]emote-debugging-port=9256' 2>/dev/null || true
          rm -f "$HOME/mmf-browser-profile/SingletonLock" "$HOME/mmf-browser-profile/SingletonSocket" "$HOME/mmf-browser-profile/SingletonCookie"
      - name: Enable Direct Post and video publish scope
        shell: bash
        run: node scripts/vps/tiktok-direct-scope.mjs
