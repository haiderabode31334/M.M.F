name: M.M.F TikTok Sandbox State Probe
on:
  push:
    branches: ["main"]
    paths:
      - "scripts/vps/tiktok-sandbox-state-probe.mjs"
      - ".github/workflows/tiktok-sandbox-state-probe.yml"
permissions:
  contents: read
concurrency:
  group: mmf-tiktok-sandbox-state-probe
  cancel-in-progress: true
jobs:
  probe:
    runs-on: [self-hosted, linux, x64]
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - name: Prepare browser
        shell: bash
        run: |
          sudo -n true
          pkill -u "$(id -u)" -f '[r]emote-debugging-port=9251' 2>/dev/null || true
          rm -f "$HOME/mmf-browser-profile/SingletonLock" "$HOME/mmf-browser-profile/SingletonSocket" "$HOME/mmf-browser-profile/SingletonCookie"
      - name: Inspect sandbox saved state safely
        shell: bash
        run: node scripts/vps/tiktok-sandbox-state-probe.mjs
