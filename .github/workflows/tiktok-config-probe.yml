name: M.M.F TikTok Config Probe

on:
  push:
    branches: ["main"]
    paths:
      - "scripts/vps/tiktok-config-probe.mjs"
      - ".github/workflows/tiktok-config-probe.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: mmf-tiktok-config-probe
  cancel-in-progress: true

jobs:
  probe:
    runs-on: [self-hosted, linux, x64]
    timeout-minutes: 6
    steps:
      - uses: actions/checkout@v4
      - name: Prepare browser
        shell: bash
        run: |
          sudo -n true
          pkill -u "$(id -u)" -f '[r]emote-debugging-port=9232' 2>/dev/null || true
          rm -f "$HOME/mmf-browser-profile/SingletonLock" "$HOME/mmf-browser-profile/SingletonSocket" "$HOME/mmf-browser-profile/SingletonCookie"
      - name: Inspect TikTok app configuration safely
        shell: bash
        run: node scripts/vps/tiktok-config-probe.mjs
