name: M.M.F TikTok Error Probe

on:
  push:
    branches: ["main"]
    paths:
      - "scripts/vps/tiktok-error-probe.mjs"
      - ".github/workflows/tiktok-error-probe.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: mmf-tiktok-error-probe
  cancel-in-progress: true

jobs:
  probe:
    runs-on: [self-hosted, linux, x64]
    timeout-minutes: 6
    steps:
      - uses: actions/checkout@v4
      - name: Prepare browser
        shell: bash
        run: |
          sudo -n true
          pkill -u "$(id -u)" -f '[r]emote-debugging-port=9234' 2>/dev/null || true
          rm -f "$HOME/mmf-browser-profile/SingletonLock" "$HOME/mmf-browser-profile/SingletonSocket" "$HOME/mmf-browser-profile/SingletonCookie"
      - name: Inspect TikTok validation errors safely
        shell: bash
        run: node scripts/vps/tiktok-error-probe.mjs
